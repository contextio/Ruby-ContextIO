require "spec_helper"

describe Contextio do
  it "has a version number" do
    expect(Contextio::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
